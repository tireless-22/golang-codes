package main

import "fmt"

// slices are most used thing in golang
// slices will depend on the arrays only
// but they functionality more than the arrays


// SLICE VS ARRAY
//The basic difference between a slice and an array 
// is that a slice is a reference to a contiguous segment of an array.
// Unlike an array, which is a value-type, slice is a reference type. 
// A slice can be a complete array or a part of an array, 
// indicated by the start and end index. 


func main(){
	fmt.Println("hello")
	var fruits=[] string{"hello"}
	// FOR slice we did not declare anyslize
	fmt.Println(fruits)
	fmt.Printf(" is %T",fruits)

	fruits=append(fruitsjl'"hi")




}







