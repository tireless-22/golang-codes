package main

import "fmt"

func main() {

	fmt.Println("hello")


	//GOOS="windows" go build
	// we can also build other operating systems
	// if we run that a executable will come
	// we can build for any os
	// if we did n't mentioned anythin then our os related executable file will be downloaded
	// go env

}